"""Stadium to Screen landing page, ported from Node to Flask.

The market model stays in the browser. The PDF is rebuilt in reportlab with
the blank-page and overlapping-text defects from the QA audit fixed rather
than carried across.
"""
