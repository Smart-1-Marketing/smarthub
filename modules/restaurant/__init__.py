"""Smart 1 restaurant landing page, running inside the Hub.

Ported from the blueprint repo. Leads go to the Hub lead panel, the OpenAI
SDK is replaced by a plain HTTP call, and the page stays public.
"""
